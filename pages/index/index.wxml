
<scroll-view scroll-y style="height: {{screenHeight}}px;" bindscrolltolower="loadMore" >
  <view bindtap="toDetail"  wx:for="{{list}}" wx:key="{{index}}" class="container"  data-aid="{{item.aid}}">
    <view class="title">{{item.title}}</view>
  </view>
</scroll-view>